export default function Addon({text}: {text: string}) {
  return <span className="inline-flex items-center rounded-l-md border border-r-0 border-gray-300 bg-gray-50 px-3 text-gray-500 sm:text-sm" >{text}</span>
}
