# MCP Server environment variables
API_URL=http://localhost:4001
API_KEY=namviek-mcp-dev-key

# Transport mode: "stdio" (local CLI) or "http" (remote hosting)
MCP_TRANSPORT=stdio

# HTTP transport settings (only used when MCP_TRANSPORT=http)
PORT=4002
ORIGINS=*
